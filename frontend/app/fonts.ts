import { Finlandica, Pacifico } from "next/font/google";

export const finlandica = Finlandica({ subsets: ["latin"], display: "swap" });

export const pacifico = Pacifico({ subsets: ["latin"], weight: ["400"], display: "swap" });